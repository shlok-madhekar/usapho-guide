{"id": "reflection-refraction-snell", "title": "Snell's law lab", "caption": "Drag the incidence angle and the two indices and watch the refracted ray swing.", "height": 260, "params": [{"key": "theta", "label": "Angle of incidence", "unit": "deg", "min": 0, "max": 89, "step": 1, "value": 35}, {"key": "n1", "label": "Index of medium 1", "unit": "", "min": 1.0, "max": 2.0, "step": 0.01, "value": 1.0}, {"key": "n2", "label": "Index of medium 2", "unit": "", "min": 1.0, "max": 2.0, "step": 0.01, "value": 1.33}], "draw": "\nvar th1 = p.theta * Math.PI / 180;\nvar n1 = p.n1, n2 = p.n2;\nvar bx = W * 0.5, by = H * 0.50;\nvar L = Math.min(W * 0.42, H * 0.44);\nctx.font = \"12px ui-sans-serif, system-ui, sans-serif\";\nctx.lineWidth = 1;\n\nfunction arrow(x0, y0, x1, y1, col) {\n  ctx.strokeStyle = col; ctx.lineWidth = 2;\n  ctx.beginPath(); ctx.moveTo(x0, y0); ctx.lineTo(x1, y1); ctx.stroke();\n  var a = Math.atan2(y1 - y0, x1 - x0), s = 7;\n  ctx.beginPath();\n  ctx.moveTo(x1, y1);\n  ctx.lineTo(x1 - s * Math.cos(a - 0.4), y1 - s * Math.sin(a - 0.4));\n  ctx.lineTo(x1 - s * Math.cos(a + 0.4), y1 - s * Math.sin(a + 0.4));\n  ctx.closePath(); ctx.fillStyle = col; ctx.fill();\n  ctx.lineWidth = 1;\n}\n\nctx.globalAlpha = 0.18;\nctx.fillStyle = C.figure;\nctx.fillRect(0, by, W, H - by);\nctx.globalAlpha = 1;\n\nctx.strokeStyle = C.rule;\nctx.beginPath(); ctx.moveTo(0, by); ctx.lineTo(W, by); ctx.stroke();\n\nctx.setLineDash([3, 4]);\nctx.strokeStyle = C.faint;\nctx.beginPath();\nctx.moveTo(bx, by - L * 1.02); ctx.lineTo(bx, by + L * 1.02);\nctx.stroke();\nctx.setLineDash([]);\n\nctx.fillStyle = C.faint;\nctx.fillText(\"normal\", bx + 5, by - L * 1.02 + 11);\nctx.fillStyle = C.ink;\nctx.fillText(\"medium 1, n = \" + n1.toFixed(2), 8, 15);\nctx.fillText(\"medium 2, n = \" + n2.toFixed(2), 8, H - 8);\n\nvar ix = bx - L * Math.sin(th1), iy = by - L * Math.cos(th1);\narrow(ix, iy, bx, by, C.accent);\nctx.fillStyle = C.accent;\nctx.fillText(\"incident\", ix + 4, iy + 12);\n\nvar rx = bx + L * Math.sin(th1), ry = by - L * Math.cos(th1);\narrow(bx, by, rx, ry, C.figure);\nctx.fillStyle = C.figure;\nctx.fillText(\"reflected\", rx - 56, ry + 12);\n\nvar s2 = n1 * Math.sin(th1) / n2;\nvar tir = s2 > 1;\nvar th2 = tir ? 0 : Math.asin(s2);\nvar tx = bx + L * Math.sin(th2), ty = by + L * Math.cos(th2);\nif (!tir) {\n  arrow(bx, by, tx, ty, C.accent);\n  ctx.fillStyle = C.accent;\n  ctx.fillText(\"refracted\", tx - 20, ty + 13);\n}\n\nctx.strokeStyle = C.faint;\nctx.beginPath(); ctx.arc(bx, by, 30, -Math.PI / 2 - th1, -Math.PI / 2); ctx.stroke();\nctx.fillStyle = C.ink;\nctx.fillText(\"t1\", bx - 20 - 14 * Math.sin(th1 / 2), by - 30 * Math.cos(th1 / 2) - 4);\nif (!tir) {\n  ctx.strokeStyle = C.faint;\n  ctx.beginPath(); ctx.arc(bx, by, 30, Math.PI / 2 - th2, Math.PI / 2); ctx.stroke();\n  ctx.fillStyle = C.ink;\n  ctx.fillText(\"t2\", bx + 8 + 16 * Math.sin(th2 / 2), by + 34 * Math.cos(th2 / 2));\n}\n\nvar u = (t * 0.55) % 2;\nvar dx, dy;\nif (u < 1) { dx = ix + (bx - ix) * u; dy = iy + (by - iy) * u; }\nelse if (tir) { dx = bx + (rx - bx) * (u - 1); dy = by + (ry - by) * (u - 1); }\nelse { dx = bx + (tx - bx) * (u - 1); dy = by + (ty - by) * (u - 1); }\nctx.fillStyle = C.ink;\nctx.beginPath(); ctx.arc(dx, dy, 4, 0, Math.PI * 2); ctx.fill();\n\nvar note = \"angle in = \" + p.theta.toFixed(0) + \" deg\";\nif (tir) {\n  note += \", no refracted ray: total internal reflection\";\n} else {\n  note += \", angle out = \" + (th2 * 180 / Math.PI).toFixed(1) + \" deg\";\n}\nif (n1 > n2) {\n  note += \", critical angle \" + (Math.asin(n2 / n1) * 180 / Math.PI).toFixed(1) + \" deg\";\n}\nout.note = note;\n"}