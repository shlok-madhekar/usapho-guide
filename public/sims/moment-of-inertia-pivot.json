{"id": "moment-of-inertia-pivot", "title": "Pivot a rod anywhere lab", "caption": "Slide the pivot and watch I and the period trade off.", "height": 300, "params": [{"key": "d", "label": "Pivot distance from centre", "unit": "m", "min": 0, "max": 0.5, "step": 0.01, "value": 0.5}, {"key": "L", "label": "Rod length", "unit": "m", "min": 0.6, "max": 1.0, "step": 0.05, "value": 1.0}, {"key": "M", "label": "Rod mass", "unit": "kg", "min": 0.5, "max": 4, "step": 0.1, "value": 1.0}], "draw": "// Uniform rod, mass M, length L, pivoted a distance d from its centre.\nvar g = 9.8;\nvar L = p.L;\nvar M = p.M;\nvar d = p.d;\nvar Icm = M * L * L / 12;\nvar I = Icm + M * d * d;\n\n// period of small oscillations; d = 0 is a free axis, no restoring torque\nvar hasSwing = d > 0.005;\nvar T = hasSwing ? 2 * Math.PI * Math.sqrt(I / (M * g * d)) : 0;\nvar theta0 = 0.28;\nvar theta = hasSwing ? theta0 * Math.cos(2 * Math.PI * t / T) : 0;\n\nctx.font = \"12px ui-sans-serif, system-ui, sans-serif\";\nctx.textBaseline = \"middle\";\n\n// ---- left panel: the swinging rod ----\nvar px = W * 0.26;\nvar py = H * 0.16;\nvar sc = (H * 0.62) / 1.7;\n\n// ceiling\nctx.strokeStyle = C.faint;\nctx.lineWidth = 4;\nctx.beginPath();\nctx.moveTo(px - W * 0.16, py);\nctx.lineTo(px + W * 0.16, py);\nctx.stroke();\n\n// vertical reference\nctx.strokeStyle = C.rule;\nctx.lineWidth = 1;\nctx.setLineDash([3, 5]);\nctx.beginPath();\nctx.moveTo(px, py);\nctx.lineTo(px, py + sc * 1.05);\nctx.stroke();\nctx.setLineDash([]);\n\nvar ux = Math.sin(theta);\nvar uy = Math.cos(theta);\nvar farEnd = L / 2 + d;\nvar nearEnd = L / 2 - d;\n\nctx.strokeStyle = C.figure;\nctx.lineWidth = 6;\nctx.lineCap = \"round\";\nctx.beginPath();\nctx.moveTo(px - ux * nearEnd * sc, py - uy * nearEnd * sc);\nctx.lineTo(px + ux * farEnd * sc, py + uy * farEnd * sc);\nctx.stroke();\nctx.lineCap = \"butt\";\n\n// centre of mass marker\nvar cmx = px + ux * d * sc;\nvar cmy = py + uy * d * sc;\nctx.fillStyle = C.accent;\nctx.beginPath();\nctx.arc(cmx, cmy, 5, 0, Math.PI * 2);\nctx.fill();\n\n// pivot\nctx.fillStyle = C.ink;\nctx.beginPath();\nctx.arc(px, py, 4, 0, Math.PI * 2);\nctx.fill();\n\n// d label\nctx.strokeStyle = C.accent;\nctx.lineWidth = 1;\nctx.beginPath();\nctx.moveTo(px, py);\nctx.lineTo(cmx, cmy);\nctx.stroke();\nctx.fillStyle = C.accent;\nctx.textAlign = \"left\";\nctx.fillText(\"d\", (px + cmx) / 2 + 7, (py + cmy) / 2);\nctx.fillStyle = C.ink;\nctx.fillText(\"pivot\", px + 8, py - 10);\nctx.fillText(\"centre of mass\", cmx + 9, cmy + 12);\n\n// ---- right panel: T against d ----\nvar gx0 = W * 0.52;\nvar gx1 = W * 0.95;\nvar gy0 = H * 0.14;\nvar gy1 = H * 0.82;\nvar dmax = L / 2;\nvar Tmax = 2 * Math.PI * Math.sqrt((Icm + M * dmax * dmax) / (M * g * dmax)) * 1.9;\n\nctx.strokeStyle = C.rule;\nctx.lineWidth = 1;\nctx.beginPath();\nctx.moveTo(gx0, gy0);\nctx.lineTo(gx0, gy1);\nctx.lineTo(gx1, gy1);\nctx.stroke();\n\nctx.strokeStyle = C.figure;\nctx.lineWidth = 2;\nctx.beginPath();\nvar started = false;\nvar dBest = L / Math.sqrt(12);\nfor (var i = 0; i <= 160; i++) {\n  var dd = dmax * (i / 160);\n  if (dd < 0.02) { continue; }\n  var TT = 2 * Math.PI * Math.sqrt((Icm + M * dd * dd) / (M * g * dd));\n  var X = gx0 + (gx1 - gx0) * (dd / dmax);\n  var Y = gy1 - (gy1 - gy0) * Math.min(TT / Tmax, 1);\n  if (!started) { ctx.moveTo(X, Y); started = true; } else { ctx.lineTo(X, Y); }\n}\nctx.stroke();\n\n// marker for the minimum\nif (dBest <= dmax) {\n  var Tb = 2 * Math.PI * Math.sqrt((Icm + M * dBest * dBest) / (M * g * dBest));\n  var bx = gx0 + (gx1 - gx0) * (dBest / dmax);\n  var by = gy1 - (gy1 - gy0) * Math.min(Tb / Tmax, 1);\n  ctx.strokeStyle = C.rule;\n  ctx.setLineDash([2, 4]);\n  ctx.beginPath();\n  ctx.moveTo(bx, by);\n  ctx.lineTo(bx, gy1);\n  ctx.stroke();\n  ctx.setLineDash([]);\n  ctx.fillStyle = C.ink;\n  ctx.textAlign = \"center\";\n  ctx.fillText(\"d = L/sqrt(12)\", bx, gy1 + 14);\n}\n\n// current point\nif (hasSwing) {\n  var cx = gx0 + (gx1 - gx0) * (d / dmax);\n  var cy = gy1 - (gy1 - gy0) * Math.min(T / Tmax, 1);\n  ctx.fillStyle = C.accent;\n  ctx.beginPath();\n  ctx.arc(cx, cy, 4.5, 0, Math.PI * 2);\n  ctx.fill();\n}\n\nctx.fillStyle = C.ink;\nctx.textAlign = \"left\";\nctx.fillText(\"period T\", gx0 + 4, gy0 - 2);\nctx.textAlign = \"right\";\nctx.fillText(\"d\", gx1, gy1 + 14);\n\nout.note = hasSwing\n  ? \"I = \" + I.toFixed(4) + \" kg m^2   (I_cm = \" + Icm.toFixed(4) +\n    \", M d^2 = \" + (M * d * d).toFixed(4) + \")   T = \" + T.toFixed(3) + \" s\"\n  : \"I = \" + Icm.toFixed(4) + \" kg m^2, the smallest possible. No gravity lever arm, so it does not swing.\";\n"}